# Cycle <N> — handoff

**Date:** <YYYY-MM-DD>
**Closed by:** pod-Claude
**Cycle:** <N>
**Status:** `done` / `in-progress` / `blocked`

## Summary

<One sentence. Where are we now?>

## What changed

- Files modified: <list>
- Commands run (one-liners): <list>
- Build artifacts produced: <list, e.g. flash_attn .so paths, env additions>
- Sentinel files written: <list>

## What worked

<Concrete: which approach succeeded, with the exact command + expected output if non-obvious.>

## What didn't

<Each failed attempt, briefly: what was tried, what happened, what was concluded. Cross-reference the log entries for full detail.>

## Open questions for architect

- <Decisions pod-Claude couldn't make alone.>
- <Things that look like they need a higher-level call.>

## Next-cycle suggestion

<What's the obvious next step? If the recipe is done, say so explicitly.>

## Pointers

- Log: `.architect/log/<YYYY-MM-DD>-cycle-<N>.md`
- Modified files (most relevant): <list>
- If recipe is locked: `.architect/RECIPE.md`
